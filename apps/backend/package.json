{
  "name": "example-auth-backend",
  "version": "1.0.0",
  "description": "Backend for Example Auth application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:setup": "psql -h localhost -U postgres -d postgres -c \"DROP DATABASE IF EXISTS reno_db\" && psql -h localhost -U postgres -d postgres -c \"CREATE DATABASE reno_db\"",
    "db:create": "psql -h localhost -U postgres -d reno_db -f database/ddl/01_schema.sql",
    "db:seed": "psql -h localhost -U postgres -d reno_db -f database/dml/02_seeds.sql",
    "db:reset": "npm run db:setup && npm run db:create && npm run db:seed"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.18.0",
    "pg-promise": "^12.6.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "express",
    "auth",
    "api"
  ],
  "author": "",
  "license": "MIT"
}
